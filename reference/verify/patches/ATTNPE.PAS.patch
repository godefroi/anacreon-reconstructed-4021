--- a/ATTNPE.PAS
+++ b/ATTNPE.PAS
@@ -22,6 +22,15 @@ USES Int,
 PROCEDURE NPEAttack(FltID,Target: IDNumber; Intent: AttackIntentionTypes; RetrIndex: Index;
                     VAR Result: AttackResultTypes);
 
+{ PATCH (fpc): test-only observability hooks, same convention as INT.PAS's ForcedRandomValue /
+  UPDATE.PAS's GetNewTotalRevIndex -- NPEAttack's real signature is untouched (NPEINTR.PAS calls
+  it for real at 4 sites now that the full SCC is linked, unlike the old trimmed lane where
+  NPEINTR.PAS was never compiled at all), so a signature-widening patch here would break those
+  real callers. NPEAttack copies its local Killed/Casualties into these globals right before
+  returning instead; read-only observability, no behavior change. }
+FUNCTION GetLastKilled: AttackArray;
+FUNCTION GetLastCasualties: AttackArray;
+
 IMPLEMENTATION
 
 {$IFDEF Debug}
@@ -383,6 +392,19 @@ PROCEDURE WorldEngage(NoOfGroups: Byte;
    {$ENDIF}
    END;  { WorldEngage }
 
+VAR
+   LastKilled,LastCasualties: AttackArray;
+
+FUNCTION GetLastKilled: AttackArray;
+   BEGIN
+   GetLastKilled:=LastKilled;
+   END;  { GetLastKilled }
+
+FUNCTION GetLastCasualties: AttackArray;
+   BEGIN
+   GetLastCasualties:=LastCasualties;
+   END;  { GetLastCasualties }
+
 PROCEDURE NPEAttack(FltID,Target: IDNumber; Intent: AttackIntentionTypes; RetrIndex: Index;
                     VAR Result: AttackResultTypes);
    VAR
@@ -396,6 +418,13 @@ PROCEDURE NPEAttack(FltID,Target: IDNumber; Intent: AttackIntentionTypes; RetrIn
       Booty: PlanetSet;
 
    BEGIN
+   { PATCH (fpc): reset the test-observability globals every call, before either branch runs --
+     the Con/Gate branch below never computes Killed/Casualties at all, and this harness's own
+     driver runs multiple cases per process invocation, so without this a Con/Gate-target case
+     would read either uninitialized stack garbage or a stale value left by a previous call. }
+   FillChar(LastKilled,SizeOf(LastKilled),0);
+   FillChar(LastCasualties,SizeOf(LastCasualties),0);
+
    HKSurprise:=ForcesUnknown(FltID,Target);
    AttEmp:=GetStatus(FltID);
    DefEmp:=GetStatus(Target);
@@ -416,6 +445,9 @@ PROCEDURE NPEAttack(FltID,Target: IDNumber; Intent: AttackIntentionTypes; RetrIn
 
       IF Intent=DestTrnAIT THEN Capture:=False ELSE Capture:=True;
       ResolveAttack(Result,FltID,Target,HKSurprise,Capture,Casualties,Killed,Booty);
+
+      LastKilled:=Killed;
+      LastCasualties:=Casualties;
       END
    ELSE IF Target.ObjTyp IN [Con,Gate] THEN
       BEGIN
