--- a/ATTACK.PAS
+++ b/ATTACK.PAS
@@ -10,13 +10,25 @@ UNIT Attack;
 
 INTERFACE
 
+{ PATCH (Phase 5 commit 5d, kept as-is by 5f): Intrface dropped from USES -- every
+  getter the procedures below need (GetStatus/GetTech/GetShips/GetCargo/GetDefns/
+  GetCapital/GetEmpireTechnology/GetBaseType/GetClass/GetRevIndex/TypeOfFleet/
+  Scouted/GetDefenseSettings/GetCoord/SetStatus/SetType/SetEfficiency/ChangeRevIndex/
+  SetCapital/SetEmpireTechnology/ChangeTotalRevIndex) already lives in PrimIntr,
+  still USES'd here. Intrface's own IMPLEMENTATION USES pulls in Fleet/Orders/NPE,
+  which in turn drag in Crt/EIO/WND -- a real GUI blast radius this harness doesn't
+  cross. Commit 5f restores ConquerWorld/ConquerEmpire/RestoreCombatant/
+  ResolveAttack (below) against local stand-ins for the six Intrface/Fleet routines
+  they call (Scout/DestroyEmpire/DestroyFleet/AbortFleet/FleetNameDestruction/
+  BalanceFleet) -- see the IMPLEMENTATION section's own PATCH comment for why
+  that's a legitimate way to check these four procedures' own directly-observable
+  effects without linking Crt. }
 USES Int,
      Types,
      Galaxy,
      DataCnst,
      Misc,
      PrimIntr,
-     Intrface,
      DataStrc,
      News;
 
@@ -94,7 +106,15 @@ PROCEDURE CalculateCombatData(Attacker: Empire; FltID,TargetID: IDNumber;
                               VAR CombatData: CombatDataRecord);
 PROCEDURE ConquerWorld(WorldID: IDNumber; Emp: Empire);
 PROCEDURE DefaultDistribution(FltID: IDNumber; VAR NoOfGroups: Byte; VAR Gp: GroupArray);
-PROCEDURE DestroyConstructionOrGate(Emp: Empire; ForcesUnk: Boolean; TargetID: IDNumber);
+{ PATCH (Phase 5 commit 5g): forward decls for DestroyConstructionOrGate/
+  HolocaustEffectiveness/HolocaustWorld dropped here, their IMPLEMENTATION bodies
+  still commented out below -- HolocaustWorld is confirmed dead code (see
+  CombatStandalone.cs's own doc comment), not ported at all; DestroyConstructionOrGate
+  is live but tested via hardcoded C# tests instead of this harness (its own
+  DestroyConstruction/DestroyStargate calls need Intrface, which this build doesn't
+  link -- see the IMPLEMENTATION section's own PATCH comment). LAMAttack is restored
+  below: it has no Rnd calls at all and needs nothing Intrface/Fleet-only, so it's a
+  clean patch-based domain (runworld.pas's own lamattack case). }
 FUNCTION EnemySurrenders(NoOfGroups: Byte;
                          VAR Gp: GroupArray;
                          VAR En: EnemyArray;
@@ -102,12 +122,6 @@ FUNCTION EnemySurrenders(NoOfGroups: Byte;
                          CombatData: CombatDataRecord): Boolean;
 FUNCTION ForcesUnknown(FltID,Target: IDNumber): Boolean;
 PROCEDURE GetEnemy(Target: IDNumber; VAR En: EnemyArray);
-FUNCTION HolocaustEffectiveness(FltID,WorldID: IDNumber): Index;
-PROCEDURE HolocaustWorld(Emp: Empire; Effectiveness: Index;
-                         WorldID,FltID: IDNumber; VAR Losses: ShipArray;
-                         VAR IndusDest: IndusArray; VAR Deaths: Population;
-                         VAR Revert: Boolean;
-                         VAR Result: HoloResultTypes);
 PROCEDURE LAMAttack(Player: Empire; LAMToUse: Resources; Target: IDNumber;
                     VAR ShipsDest: ShipArray;
                     VAR DefnsDest: DefnsArray);
@@ -120,8 +134,53 @@ PROCEDURE RestoreCombatant(ObjID: IDNumber; VAR Casualties: AttackArray);
 
 IMPLEMENTATION
 
-USES
-	Fleet;
+{ PATCH (Phase 5 commit 5d, kept as-is by 5f): "USES Fleet;" dropped here -- no
+  procedure kept in this build (5f included) touches Fleet directly; the six
+  Fleet/Intrface routines ConquerWorld/ConquerEmpire/RestoreCombatant/
+  ResolveAttack call are given local stand-ins below instead. }
+
+{ PATCH (Phase 5 commit 5f): local stand-ins for Scout/DestroyEmpire (normally
+  Intrface) and DestroyFleet/AbortFleet/FleetNameDestruction/BalanceFleet (normally
+  Fleet/Intrface) -- their real bodies need Crt/EIO/WND through Intrface's own
+  IMPLEMENTATION USES, which this harness can't link (see the INTERFACE section's
+  own PATCH comment). ConquerWorld/ConquerEmpire/RestoreCombatant/ResolveAttack
+  below are restored verbatim from source; this harness's combat/npeattack cases
+  only assert on their own directly-observable effects (world
+  ownership/efficiency/RevIndex, Casualties/Killed, Result, Booty) -- not on what
+  these six stand-ins would really do (scouting a newly-conquered world, tearing
+  down a destroyed empire's fleets/starbases, transferring a destroyed fleet's
+  cargo to whatever's co-located, rebalancing an over-capacity fleet's cargo). Those
+  stay covered by direct source reading plus C# unit tests instead (see
+  docs/ROADMAP.md's Phase 5 commit 5f notes). BalanceFleet in particular is a no-op
+  here regardless: no golden case ever gives RestoreCombatant's Flt branch a fleet
+  whose FleetCargoSpace goes negative (only casualties -- i.e. fewer ships/cargo --
+  are ever applied here, which can only free up space, never exceed it). }
+PROCEDURE Scout(Emp: Empire; XY: XYCoord);
+   BEGIN
+   END;
+
+PROCEDURE DestroyEmpire(Emp: Empire);
+   BEGIN
+   END;
+
+PROCEDURE DestroyFleet(FltID: IDNumber);
+   BEGIN
+   END;
+
+PROCEDURE AbortFleet(Flt1,GroundID: IDNumber; Report: Boolean);
+   BEGIN
+   END;
+
+PROCEDURE FleetNameDestruction(Emp: Empire; FltID: IDNumber; VAR NewLoc: Location);
+   BEGIN
+   NewLoc.XY:=Limbo;
+   NewLoc.ID:=FltID;
+   END;
+
+PROCEDURE BalanceFleet(VAR Sh: ShipArray; VAR Cr: CargoArray);
+   BEGIN
+   END;
+
 
 CONST
    { CombatTechAdj:
@@ -1436,190 +1495,17 @@ FUNCTION ForcesUnknown(FltID,Target: IDNumber): Boolean;
       ForcesUnknown:=False;
    END;  { ForcesUnknown }
 
-FUNCTION HolocaustEffectiveness(FltID,WorldID: IDNumber): Index;
-   VAR
-      TotalAttack,TotalDefense: LongInt;
-      Sh,FltSh: ShipArray;
-      Df: DefnsArray;
-
-   BEGIN
-   GetDefns(WorldID,Df);
-   GetShips(WorldID,Sh);
-   TotalDefense:=MilitaryPower(Sh,Df);
-
-   FillChar(Df,SizeOf(Df),0);
-   GetShips(FltID,FltSh);
-   TotalAttack:=MilitaryPower(FltSh,Df);
-
-   IF TotalDefense=0 THEN
-      HolocaustEffectiveness:=100
-   ELSE IF (TotalAttack<5000) OR (TotalAttack/TotalDefense<1) THEN
-      HolocaustEffectiveness:=0
-   ELSE
-      HolocaustEffectiveness:=LesserInt(100,Round(10*TotalAttack/TotalDefense));
-   END;  { HolocaustEffectiveness }
-
-PROCEDURE HolocaustWorld(Emp: Empire; Effectiveness: Index;
-                         WorldID,FltID: IDNumber; VAR Losses: ShipArray;
-                         VAR IndusDest: IndusArray; VAR Deaths: Population;
-                         VAR Revert: Boolean;
-                         VAR Result: HoloResultTypes);
-{ HolocaustWorld ---------------------------------------------------------------
-   This procedure will carry out a nuclear attack on the population and
-   industry of a world.
------------------------------------------------------------------------------- }
-   VAR
-      Indus: IndusArray;
-      IndI: IndusTypes;
-      RevChange,EnemyRev: Integer;
-      EnemyEmp: Empire;
-      Loc: Location;
-      Pop: Population;
-
-   FUNCTION WorldSurrenders(Emp: Empire; Effectiveness: Index; WorldID: IDNumber): Boolean;
-      VAR
-         Tech: TechLevel;
-         Pop: Population;
-         ChanceToSurrender: Real;
-
-      BEGIN
-      Tech:=GetTech(WorldID);
-      Pop:=GetPopulation(WorldID);
-      ChanceToSurrender:=(Sqr(Effectiveness/100));
-
-      IF (Pop>1000) THEN
-         ChanceToSurrender:=ChanceToSurrender/2;
-
-      IF Tech<PreWrpLvl THEN
-         WorldSurrenders:=True
-      ELSE IF Tech>StrTchLvl THEN
-         WorldSurrenders:=False
-      ELSE IF Random(1)<=ChanceToSurrender THEN
-         WorldSurrenders:=True
-      ELSE
-         WorldSurrenders:=False;
-      END;  { WorldSurrenders }
-
-   PROCEDURE CalculateDeaths(VAR Pop: Population; Effectiveness: Index;
-                             VAR Deaths: Population);
-      BEGIN
-      Deaths:=Round((Pop/Rnd(850,1200))*Effectiveness);
-      Pop:=Pop-Deaths;
-      END;  { CalculateDeaths }
-
-   PROCEDURE CalculateDestruction(VAR Indus: IndusArray; Effectiveness: Index;
-                                  VAR IndusDest: IndusArray);
-      VAR
-         IndI: IndusTypes;
-         PercentDamage: Real;
-
-      BEGIN
-      PercentDamage:=(Effectiveness/Rnd(200,800));
-      FOR IndI:=BioInd TO TriInd DO
-         BEGIN
-         IndusDest[IndI]:=Round(Indus[IndI]*PercentDamage);
-         Dec(Indus[IndI],IndusDest[IndI]);
-         END;
-      END;  { CalculateDestruction }
-
-   PROCEDURE AttackerLosses(FltID: IDNumber; Effectiveness: Index;
-                            VAR Losses: ShipArray);
-      VAR
-         ShpI: ShipTypes;
-         FuelCap,PercentDamage: Real;
-         Sh: ShipArray;
-
-      BEGIN
-      FillChar(Losses,SizeOf(Losses),0);
-      PercentDamage:=(100-Effectiveness)/Rnd(600,1000);
-      GetShips(FltID,Sh);
-
-      FOR ShpI:=fgt TO trn DO
-         IF (ShpI<>jtn) AND (ShpI<>trn) THEN
-            BEGIN
-            Losses[ShpI]:=Round(Sh[ShpI]*PercentDamage*CombatTable[GDM,ShpI]/100);
-            Dec(Sh[ShpI],Losses[ShpI]);
-            END;
-
-      FuelCap:=FuelCapacity(Sh);
-      IF GetFleetFuel(FltID)>FuelCap THEN
-         SetFleetFuel(FltID,FuelCap);
-      END;  { AttackLosses }
-
-   PROCEDURE RevertTechnology(WorldID: IDNumber; Effectiveness: Index; 
-                              VAR Revert: Boolean);
-      BEGIN
-      IF Rnd(1,100)<=(Effectiveness DIV 2) THEN
-         BEGIN
-         Revert:=True;
-         SetTech(WorldID,PreTchLvl);
-         END
-      ELSE
-         Revert:=False;
-      END;  { RevertTechnology }
-
-   { HolocaustWorld }
-   BEGIN
-   FillChar(Losses,SizeOf(Losses),0);
-   FillChar(IndusDest,SizeOf(IndusDest),0);
-   Deaths:=0;
-   EnemyEmp:=GetStatus(WorldID);
-
-   IF (EnemyEmp=Indep) AND WorldSurrenders(Emp,Effectiveness,WorldID) THEN
-      BEGIN
-      Result:=WorldSurrendersHRT;
-      ConquerWorld(WorldID,Emp);
-
-      { Change revolution of world }
-      ChangeRevIndex(WorldID,Rnd(20,45));
-
-      { Change revolution of empire }
-      IF Rnd(1,100)<=50 THEN
-         RevChange:=Rnd(15,25)
-      ELSE
-         RevChange:=-Rnd(1,10);
-      END
-   ELSE
-      BEGIN
-      Result:=WorldDestroyedHRT;
-
-      Pop:=GetPopulation(WorldID);
-      CalculateDeaths(Pop,Effectiveness,Deaths);
-      SetPopulation(WorldID,Pop);
-
-      GetIndus(WorldID,Indus);
-      CalculateDestruction(Indus,Effectiveness,IndusDest);
-      PutIndus(WorldID,Indus);
-
-      SetEfficiency(WorldID,Rnd(1,10));
-
-      RevertTechnology(WorldID,Deaths,Revert);
-
-      AttackerLosses(FltID,Effectiveness,Losses);
-
-      { Change revolution of enemy empire }
-      EnemyRev:=(Deaths DIV 7)+Rnd(1,10);
-      IF EnemyRev>30 THEN
-         EnemyRev:=30;
-
-      { Change revolution of empire }
-      RevChange:=(Deaths DIV 6)+Rnd(-15,3);
-      IF RevChange>50 THEN
-         RevChange:=50;
-
-      { Send news }
-      Loc.ID:=WorldID;  Loc.XY:=Limbo;
-      AddNews(EnemyEmp,WHolo,Loc,Ord(Emp),0,0);
-      AddNews(EnemyEmp,DthHolo,Loc,Deaths,0,0);
-      FOR IndI:=BioInd TO TriInd DO
-         IF IndusDest[IndI]>0 THEN
-            AddNews(EnemyEmp,IndDs,Loc,Indus[IndI],Ord(IndI),0);
-      END;
-
-   ChangeTotalRevIndex(Emp,RevChange);
-   ChangeTotalRevIndex(EnemyEmp,EnemyRev);
-   END;  { HolocaustWorld }
-
+{ PATCH (Phase 5 commit 5g): HolocaustEffectiveness/HolocaustWorld bodies stay removed
+  here -- confirmed dead code (see CombatStandalone.cs's own doc comment: even
+  HolocaustCommand's own forward interface declaration in MSCCOMM.PAS is commented
+  out, in both the 1.31 and 2.0 source trees), not ported at all.
+  DestroyConstructionOrGate's body also stays removed: it's live (wired into this
+  port's NPEAttack), but its own DestroyConstruction/DestroyStargate calls live in
+  Intrface, which this build doesn't link (see the INTERFACE section's own PATCH
+  comment) -- tested via hardcoded C# tests instead (CombatStandaloneTests.cs), not
+  this harness. LAMAttack (below) needs neither Intrface nor Fleet -- every getter/
+  setter it calls is already in PrimIntr/Misc/DataCnst/News, all in this unit's USES
+  clause -- so it's restored verbatim as runworld.pas's own lamattack domain. }
 PROCEDURE LAMAttack(Player: Empire; LAMToUse: Resources; Target: IDNumber;
                     VAR ShipsDest: ShipArray;
                     VAR DefnsDest: DefnsArray);
@@ -1703,33 +1589,4 @@ PROCEDURE LAMAttack(Player: Empire; LAMToUse: Resources; Target: IDNumber;
    AddGlobalNews([Emp,Player],Target,GLBLAMStrk,Loc,Ord(Player),Ord(Emp),0);
    END;  { LAMAttack }
 
-PROCEDURE DestroyConstructionOrGate(Emp: Empire; ForcesUnk: Boolean; TargetID: IDNumber);
-   VAR
-      EnemyEmp: Empire;
-      Loc: Location;
-
-   BEGIN
-   Loc.ID:=EmptyQuadrant;  GetCoord(TargetID,Loc.XY);
-   EnemyEmp:=GetStatus(TargetID);
-
-   CASE TargetID.ObjTyp OF
-      Con: BEGIN
-         DestroyConstruction(TargetID);
-         ChangeTotalRevIndex(EnemyEmp,Rnd(3,7));
-         IF ForcesUnk THEN
-            AddNews(EnemyEmp,ConDsUNK,Loc,0,0,0)
-         ELSE
-            AddNews(EnemyEmp,ConDs,Loc,Ord(Emp),0,0);
-         END;
-      Gate: BEGIN
-         DestroyStargate(TargetID);
-         ChangeTotalRevIndex(EnemyEmp,Rnd(7,15));
-         IF ForcesUnk THEN
-            AddNews(EnemyEmp,GteDsUNK,Loc,0,0,0)
-         ELSE
-            AddNews(EnemyEmp,GteDs,Loc,Ord(Emp),0,0);
-         END;
-      END;
-   END;  { DestroyConstructionOrGate }
-
 END.
