--- a/NEWS.PAS
+++ b/NEWS.PAS
@@ -165,7 +165,10 @@ PROCEDURE GetOpenSlot(Player: Empire; VAR NewSlot: NewsRecordPtr);
    BEGIN
    WITH NewsData[Player] DO
       BEGIN
-      IF MaxAvail>20 THEN
+      { PATCH (fpc): MaxAvail (TP's real-mode heap-free check) has no equivalent
+        under virtual memory -- the "not enough heap" branch below is effectively
+        unreachable on any modern target, so the guard is always true here. }
+      IF True THEN
          BEGIN
          New(NewSlot);
 
