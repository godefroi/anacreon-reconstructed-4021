# Smoke-test script for Reconstructed4021.TuiDriver, run against a scratch fixture
# (assets/saves/Resupply Smoke Test.json, NOT committed -- Border Skirmish.json with planet (4,4)
# reassigned to Human and a Human fleet (20 transports, 10 fuel) placed at the capital (0,0)):
#
#   dotnet run --project src/Reconstructed4021.TuiDriver -- \
#     --load "assets/saves/Resupply Smoke Test.json" --script assets/tui-driver-scripts/resupply-smoke-test.txt
#
# Fleet > Resupply: cursor starts at the capital (0,0), where the one fleet already sits, so
# PickOwnFleetAtCursor auto-picks it. Enter picks (0,0) as the source world (the two worlds are now
# picked back to back). Right x4 + Down x4 moves the cursor to (4,4), Enter picks it as the
# destination. The cargo-type-and-amount window opens next, list focused on Legion (index 0); Down x3
# moves to Chemicals, whose row already shows what's available at the source. Enter moves focus down
# into the amount field, pre-filled with that type's own max (the fleet's own 20 transports cap it at
# 60 tons of chemicals, comfortably under the 500 sitting at the source) -- a second Enter accepts that
# default and commits. Fleet > Orders afterward would show the compiled DEST/TRAN/REFU sequence.
f
u
SLEEP 200
DUMP
Enter
SLEEP 200
DUMP
Right
Right
Right
Right
Down
Down
Down
Down
SLEEP 200
Enter
SLEEP 200
DUMP
Down
Down
Down
SLEEP 100
DUMP
Enter
SLEEP 200
DUMP
Enter
SLEEP 200
DUMP
