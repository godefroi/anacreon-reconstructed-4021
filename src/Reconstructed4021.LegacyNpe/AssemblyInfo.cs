using System.Runtime.CompilerServices;

// Reconstructed4021.Tests needs KingdomTurnHandler's internal (NpeEmpireType, NpeCharacter, ...)
// constructor to build a handler from an arbitrary evolved gene vector (GeneticAlgorithmTests) rather
// than only the public constructor's own random persona roll.
[assembly: InternalsVisibleTo("Reconstructed4021.Tests")]
